@{
    ViewBag.Title = "About";
}

<h3>@ViewBag.Message</h3>

<p>GitRepoToJSON is a small ASP.NET Application in C# that fetch data from Github with there API and put it in a JSON File</p>
<p>TagManuel is nearly the same, but with other id data set</p>
